spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update  # 스키마 유지
    properties:
      hibernate:
        format_sql: true
    show-sql: true

jwt:
  secret: realWorldByGiro123456789qwerasdfrealWorldByGiro123456789qwerasdf
  expiration: 86400000